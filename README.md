# Expense-Tracker-System
I built a multi-user desktop expense tracker using Java Swing for my OOP project. It demonstrates key concepts like interfaces, multi-threading, and GUI concepts. Features include add, edit, delete, undo, search, currency switching, and auto-save every 30 seconds, with all data stored locally in CSV files so that it works offline.
